<% layout("layouts/boilerplate"); %>

<div class="row mt-5">
  <div class="alert alert-danger col-6 offset-3 text-center" role="alert">
    <h4 class="alert-heading">Error <%= statusCode %></h4>
    <p><%= message || "Something went wrong." %></p>
    <a href="/listings" class="btn btn-outline-danger mt-3">← Go Back</a>
  </div>
</div>
